import { React, shallow } from '../test.config'
import { findAttribute } from '../test.helpers';

describe('Feature:', () => {

  describe('Positive Scenarios', () => {

    it.skip('can run a test', () => {
      expect(true).to.equal(true);
    });

  });

  describe('Negative Scenarios', () => {
  });

});